classDiagram
    class Book {
        -String name
        -String publisher
        -ArrayList~Person~ authors
        +ArrayList~Person~ getAuthors()
        +void addAuthor(Person author)
    }
    
    class Person {
        -String name
        -int age
        +Person(String initialName)
        +void printPerson()
        +String getName()
    }
    
    Book "1" --> "0..*" Person : authors
    
    note for Book "Book 클래스는 여러 Person 객체를 authors로 가질 수 있음"
    note for Person "Person 클래스는 이름과 나이를 가지는 기본 클래스"
